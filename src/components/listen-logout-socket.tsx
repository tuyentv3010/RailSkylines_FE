// import { useLogoutMutation } from "@/queries/useAuth";
// Socket.io logout listener removed; Pusher does not require this.
// Keep component stub to avoid import errors.

export default function ListenLogoutSocket() {
  return null;
}
